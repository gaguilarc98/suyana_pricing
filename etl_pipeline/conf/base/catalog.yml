# Here you can define all your datasets by using simple YAML syntax.
#
# Documentation for this file format can be found in "The Data Catalog"
# Link: https://docs.kedro.org/en/stable/catalog-data/introduction/#data-catalog
# Here you can define all your datasets by using simple YAML syntax.

# Arguments
# Use snake_case style for uniformity e.g. costa_rica, 'swiss_re', 'bisa_seguros'
_arg:
  country: ${runtime_params:country, ${globals:country}}
  region: ${runtime_params:region, ${globals:region}}
  lead_id: ${runtime_params:lead_id, 'valles'}
  provider: ${runtime_params:provider, ${globals:provider}}
  field: ${runtime_params:field, ${globals:field}}
  version: ${runtime_params:version, ${globals:version}}

# Geometry Data
gdf_request:
  type: geopandas.GenericDataset
  filepath: ${globals:data_path}/geometries/${_arg.country}_polygons.parquet
  file_format: parquet
  save_args:
    compression: GZIP

# Planet source data (pre-existing in S3, partitioned by location_id)
planet_amsre:
  type: pricing_automation.datasets_registry.ZarrPartitionedDataset
  filepath: ${globals:data_path}/${_arg.country}/${_arg.region}/sources/PLANET_AMSRE_swc_<partition>.zarr
  load_args:
    combine: by_coords
    parallel: true

planet_amsr2:
  type: pricing_automation.datasets_registry.ZarrPartitionedDataset
  filepath: ${globals:data_path}/${_arg.country}/${_arg.region}/sources/PLANET_AMSR2_swc_<partition>.zarr
  load_args:
    combine: by_coords
    parallel: true

era5_gap:
  type: pricing_automation.datasets_registry.ZarrPartitionedDataset
  filepath: ${globals:data_path}/${_arg.country}/${_arg.region}/sources/ERA5_swc_<partition>.zarr
  load_args:
    combine: by_coords
    parallel: true

# Geometry Data:
dict_bounds:
  type: MemoryDataset
  #type: json.JSONDataset  # Note: Use JSONDataSet from kedro_datasets
  #filepath: data/${_arg.country}/my_data.json
  
gdf_aoi:
  type: geopandas.GenericDataset
  filepath: ${globals:data_path}/${_arg.country}/${_arg.region}/geometries/location_geometries.parquet 
  file_format: parquet #Important if not provided kedro will attempt to use GDAL which does not support S3 natively
  save_args:
    compression: GZIP
  #  driver: GPKG
  #load_args:
  #  driver: GPKG
  #  encoding: UTF-8

# Request data

ds_request:
  #type: pricing_automation.datasets_registry.NetCDFPartitionedDataset
  #filepath: data/${_arg.country}/sources/${_arg.provider}/${_arg.field}_<partition>.nc
  type: pricing_automation.datasets_registry.ZarrPartitionedDataset
  filepath: ${globals:data_path}/${_arg.country}/${_arg.region}/sources/${_arg.provider}_${_arg.field}_<partition>.zarr
  save_args:
    consolidated: true
    mode: w
    zarr_format: 2
  load_args:
    combine: by_coords
    parallel: true
    drop_variables: ['expver', 'number']

# Index Data
ds_processed:
  type: pricing_automation.datasets_registry.XarrayZarrDataset
  filepath: ${globals:data_path}/${_arg.country}/${_arg.region}/features/${_arg.provider}_${_arg.field}_anomalies.zarr #${_arg.version}.zarr
  save_args:
    consolidated: true
    mode: w
    zarr_format: 2
    append_dim: time
    chunks: {'time': -1, 'lat': -1, 'lon': -1}
    safe_chunks: false
  load_args:
    consolidated: true

ds_climatology:
  type: pricing_automation.datasets_registry.XarrayZarrDataset
  filepath: ${globals:data_path}/${_arg.country}/${_arg.region}/features/${_arg.provider}_${_arg.field}_climatology.zarr
  save_args:
    consolidated: true
    mode: a
    append_dim: location_id
  load_args:
    consolidated: true

# Summarized Data
df_pixels:
  type: pandas.ParquetDataset
  filepath: ${globals:data_path}/${_arg.country}/${_arg.region}/features/${_arg.lead_id}/${_arg.provider}_${_arg.field}_pixel_assignment.parquet
  #type: pricing_automation.datasets_registry.DeltaLakeDataset
  #filepath: ${globals:data_path}/${_arg.country}/${_arg.region}/features/${_arg.lead_id}/${_arg.provider}_${_arg.field}_pixel_assignment
  #save_args:
  #  mode: append

df_cluster: 
  #type: pandas.ParquetDataset
  #filepath: ${globals:data_path}/${_arg.country}/${_arg.region}/outputs/${_arg.lead_id}/${_arg.provider}_${_arg.field}_daily_indices.parquet 
  #save_args:
  #  compression: GZIP
  type: pricing_automation.datasets_registry.DeltaLakeDataset
  filepath: ${globals:data_path}/${_arg.country}/${_arg.region}/outputs/${_arg.lead_id}/${_arg.provider}_${_arg.field}_daily_indices
  save_args:
    mode: append

# Indices Data by Window
df_indices: 
  type: pandas.ParquetDataset
  filepath: ${globals:data_path}/${_arg.country}/${_arg.region}/outputs/${_arg.lead_id}/${_arg.provider}_${_arg.field}_window_indices.parquet
  save_args:
    compression: GZIP

pkl_fit: 
  type: pickle.PickleDataset
  filepath: ${globals:data_path}/${_arg.country}/${_arg.region}/outputs/${_arg.lead_id}/${_arg.provider}_${_arg.field}_model_fit.pkl
  backend: pickle

# Payouts and Policy Data by Window
df_payouts:
  type: pandas.ParquetDataset
  filepath: ${globals:data_path}/${_arg.country}/${_arg.region}/outputs/${_arg.lead_id}/${_arg.provider}_${_arg.field}_payouts.parquet
  save_args:
    compression: GZIP

df_policy:
  type: pandas.ParquetDataset
  filepath: ${globals:data_path}/${_arg.country}/${_arg.region}/outputs/${_arg.lead_id}/${_arg.provider}_${_arg.field}_policy.parquet
  save_args:
    compression: GZIP

# AEP Data
df_annual_agg: 
  type: pandas.ParquetDataset
  filepath: ${globals:data_path}/${_arg.country}/${_arg.region}/outputs/${_arg.lead_id}/aggregate_annual_losses.parquet
  save_args:
    compression: GZIP

df_aep:
  type: pandas.ParquetDataset
  filepath: ${globals:data_path}/${_arg.country}/${_arg.region}/outputs/${_arg.lead_id}/aep_curves.parquet
  save_args:
    compression: GZIP

# Quote Data
df_pricing: 
  type: pandas.ParquetDataset
  filepath: ${globals:data_path}/${_arg.country}/${_arg.region}/outputs/${_arg.lead_id}/pricing_quote.parquet
  save_args:
    compression: GZIP

plt_portfolio:
  type: matplotlib.MatplotlibDataset
  filepath: ${globals:data_path}/${_arg.country}/${_arg.region}/displays/${_arg.lead_id}/aep_portfolio.png
  save_args:
    bbox_inches: tight
    dpi: 150

plt_aep:
  type: matplotlib.MatplotlibDataset
  filepath: ${globals:data_path}/${_arg.country}/${_arg.region}/displays/${_arg.lead_id}/aep_per_crop.png
  save_args:
    bbox_inches: tight
    dpi: 150

# Plot Outputs
plt_variability_map:
  type: matplotlib.MatplotlibDataset
  filepath: ${globals:data_path}/${_arg.country}/${_arg.region}/displays/${_arg.lead_id}/variability_map.png
  save_args:
    bbox_inches: tight
    dpi: 150

plt_trend_map:
  type: matplotlib.MatplotlibDataset
  filepath: ${globals:data_path}/${_arg.country}/${_arg.region}/displays/${_arg.lead_id}/trend_map.png
  save_args:
    bbox_inches: tight
    dpi: 150

plt_trigger_freq_map:
  type: matplotlib.MatplotlibDataset
  filepath: ${globals:data_path}/${_arg.country}/${_arg.region}/displays/${_arg.lead_id}/trigger_frequency_map.png
  save_args:
    bbox_inches: tight
    dpi: 150

plt_anomaly_timeseries:
  type: matplotlib.MatplotlibDataset
  filepath: ${globals:data_path}/${_arg.country}/${_arg.region}/displays/${_arg.lead_id}/anomaly_timeseries.png
  save_args:
    bbox_inches: tight
    dpi: 150

plt_activation_map:
  type: matplotlib.MatplotlibDataset
  filepath: ${globals:data_path}/${_arg.country}/${_arg.region}/displays/${_arg.lead_id}/activation_map.png
  save_args:
    bbox_inches: tight
    dpi: 150

plt_time_series:
  type: matplotlib.MatplotlibDataset
  filepath: ${globals:data_path}/${_arg.country}/${_arg.region}/displays/${_arg.lead_id}/indices_timeseries.png
  save_args:
    bbox_inches: tight
    dpi: 150